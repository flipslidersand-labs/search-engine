line-length = 100

[lint]
# BLE001 (blind except Exception) / S110 (try-except-pass) は無効化。
# このコードベースは #74 の調査で「意図的なフォールバック」と確認済み
# （モデルロード失敗時のデフォルト値、リモートサービス不達時のローカル
# フォールバック、環境依存インポート等）。個別に例外型を絞る改修は
# 挙動変更を伴うためスコープ外とする。
ignore = ["BLE001", "S110"]
